# Optional dependency: only required for the "Generate Study Guide" PDF
# feature. The rest of the CISSP Coach app runs fine without Python.
#
# Install with:  python -m pip install -r requirements.txt
reportlab>=4.0,<5.0
